<!doctype html>
<html>
 <head>
   <meta charset="UTF-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
 </head>
 <body>
    {% include "components/navbar.html.jinja" %}
  
    <div class="bg-white">
    <h1>
    <img class= max-h-100 max-w-100 border-black border-10px src=" {{ product ['Image'] }}">
        {{ product["Name"] }}<br>
        ${{ product["Price"] }}<br>
        {{ product ["Size"]}}<br>
        {{ product ["Category"]}}<br>

        <button class= "ml-150 bg-red-600 cursor-pointer pl-4 pr-4 text-l text-white ">
            Add to Cart
        </button>
    </h1>    
    </div>
   
 </body>
</html>